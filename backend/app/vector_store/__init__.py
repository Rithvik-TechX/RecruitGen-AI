"""
Vector Store Layer — Embedding Storage & Similarity Search.

Manages vector database interactions for semantic search
across resumes, job descriptions, and candidate profiles.

Future contents:
- client.py       → Vector DB client (Pinecone / pgvector / Qdrant)
- embeddings.py   → Text → embedding pipeline
- search.py       → Similarity search helpers
"""
