/**
 * Mock data has been removed.
 * All pages now use real API hooks from @/lib/hooks.
 * This file is kept as an empty placeholder to avoid import errors
 * in case any file still references it.
 */

export {};
